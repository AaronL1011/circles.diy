# Docmost Configuration
# Copy this to .env.docmost and update the values

# REQUIRED: Generate a long random secret (32+ characters)
# You can use: openssl rand -base64 32
DOCMOST_APP_SECRET=REPLACE_WITH_LONG_SECRET

# REQUIRED: Strong database password (16+ characters)
# You can use: openssl rand -base64 16
DOCMOST_DB_PASSWORD=STRONG_DB_PASSWORD

# Your domain (will be set automatically by init script)
DOCMOST_DOMAIN=docs.yourdomain.com